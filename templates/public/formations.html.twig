{% extends 'layout/layout.html.twig' %}
{% block description %}
CSLoisirsCréatifs conception de meubles en carton. Voici les dates des stages disponibles pour cette année. Tout les niveaux sont acceptés.
{% endblock %}

{% block title %}
CSLoisirsCréatifs - Stages
{% endblock %}

{% block container %}
<section>
    <div class="wrapper-grid-container m-top-3">
        <div class="wrapper-header">
            <h1>Stages</h1>
        </div>
        <div class="formations-content">
            {% for formation in formations%}
                <div class="formation-details-container">
                    <div class="img-formation{%  if loop.index is divisible by (2) %} divisible {% endif %}">
                        <img src="{{ asset('assets/images/') ~ formation.area.name|lower ~ '.jpg' }}" class="img-fluid">
                    </div>
                    <div class="formation-details{%  if loop.index is divisible by (2) %} divisible {% endif %}">
                        <h2>{{ formation.title }}</h2>
                        <p>{{ formation.description | nl2br}}</p>
                        <p>Prix de la formaiton : {{ formation.price }} €</p>
                        {% if formation.availableSeats is not null %}
                        <p><strong>Nombre de places disponible : {{ formation.availableSeats }}</strong></p>
                        {% endif %}
                        <h3>Adresse du stage</h3>
                        <p class="adress-formation">{{ formation.area.name }}</p>
                        <p class="adress-formation">{{ formation.area.address }}</p>
                        <p class="adress-formation">{{ formation.area.zip }}</p>
                        <p class="adress-formation">{{ formation.area.city }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</section>
{% endblock %}